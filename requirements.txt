# 自動化生成 — 與 pyproject.toml [project.dependencies] 保持同步
# 主要依賴的權威來源是 pyproject.toml + uv.lock；本檔提供給沒裝 uv、
# 想直接 pip install -r 的環境作備援。
# v1.5.4 加嚴：跟 Dependabot 22 alerts 對齊
# starlette>=0.49.3: Range header O(n²) DoS in FileResponse
# python-multipart>=0.0.18: arbitrary file write + DoS
# jinja2>=3.1.6: 3 個 sandbox breakout
# Pillow>=12.2.0: 6 個 high CVE (FITS / PSD / PDF Trailer / Font OF / nested coords)
# fastapi>=0.124: 才支援 starlette 0.49+
fastapi>=0.124.0,<0.140
uvicorn[standard]>=0.32.0,<0.40
starlette>=0.49.3,<0.53
python-multipart>=0.0.18,<0.1
jinja2>=3.1.6,<4
pydantic>=2.9.0,<3
pydantic-settings>=2.5.0,<3
PyMuPDF>=1.26.0,<2
pymupdf4llm>=0.3.0,<0.4
Pillow>=12.2.0,<13
pdfplumber>=0.11.0
pyzipper>=0.4.0
httpx>=0.27,<1
# idna>=3.15: CVE-2026-45409 idna.encode() DoS (transitive via httpx/fastapi/starlette)
idna>=3.15,<4
python-docx>=1.0.0
odfpy>=1.4
ldap3>=2.9.1,<3
# 軟依賴 — pdf-editor OCR fallback；missing 時退回「請手動重打」訊息
pytesseract>=0.3.10,<0.4
# EasyOCR — 主 OCR 引擎（CJK 識別率優於 tesseract）；JaidedAI 泰國公司
# Pulls in PyTorch (~700MB)，install 時間會較久。失敗 graceful 降回 tesseract
easyocr>=1.7,<2
# 主機資源監控 — admin /admin/system-status 頁顯示 CPU / RAM / disk / IO
psutil>=5.9,<7
# TOTP 2FA — 稽核員角色強制 + 其他角色可選
pyotp>=2.9,<3
qrcode>=7.4,<9
# einvoice-scan QR Code 解碼（v1.7.78+）— Linux/macOS 需 zbar shared lib（apt/brew）
pyzbar>=0.1.9,<0.2
# pdf-to-office (v1.8.32+, Sprint 1) — pdf2docx 鎖 0.5.13（上游停止維護，已 fork 計畫），
# rapidfuzz 給 docx↔PDFTruth 模糊比對
pdf2docx>=0.5.13,<0.6
rapidfuzz>=3.0,<4
# translate-doc xlsx 匯出（v1.7.51）— pyproject 有但本檔之前漏列
openpyxl>=3.1,<4
# 直接 import 的套件，顯式鎖版（numpy: pdf-to-office fixer + ocr 前處理；
# lxml: pdf-to-office docx anchor XML）。原本只靠 transitive 帶入。
numpy>=1.26,<3
lxml>=4.9,<7
